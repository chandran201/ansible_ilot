---
- set_fact:
    pd_core_standalone_packages:
      - "{{ config.siteName}}-subcap-application-rpm-internal"

- name: Remove application packages
  yum:
    name: "{{ item }}"
    state: removed
    disable_gpg_check: yes
  with_items: "{{ pd_core_standalone_packages }}"

- name: Launch yum clean all
  shell: "yum clean all"
  become: yes

- name: Install application packages from yum repo
  yum:
    name: "{{ item }}-{{ config.ilottery_version}}"
    state: present
    disable_gpg_check: yes
  with_items: "{{ pd_core_standalone_packages }}"

- name: Create /etc/gtech/pd/config/subcap-internal folder
  file:
    path: /etc/gtech/pd/config/subcap-internal
    state: directory
    owner: springboot
    group: springboot
    mode: 0755

- name: Create /var/log/subcap-internal/ folder
  file:
    path: /var/log/subcap-internal
    state: directory
    owner: springboot
    group: springboot
    mode: 0755

- name: Create symlinks /opt/jdk to /usr/lib/jvm/java
  file:
    src: "/usr/lib/jvm/java"
    dest: "/opt/jdk"
    state: link

- name: create /usr/local/env
  file:
    path: "/usr/local/env"
    state: directory
    mode: 0755

- name: "Install env to /usr/local/env/"
  template:
    src: "env/subcap-internal"
    dest: "/usr/local/env/subcap-internal"
    owner: "springboot"
    group: "springboot"
    mode: 0755
