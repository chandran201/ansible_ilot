---
- set_fact:
    dc_adapter_packages:
      - "{{ config.siteName}}-dc-adapter-application-rpm"

- name: Remove application packages
  yum:
    name: "{{ item }}"
    state: removed
    disable_gpg_check: yes
  with_items: "{{ dc_adapter_packages }}"

- name: Launch yum clean all
  shell: "yum clean all"
  become: yes

- name: Install application packages from yum repo
  yum:
    name: "{{ item }}-{{ config.ilottery_version}}"
    state: present
    disable_gpg_check: yes
  with_items: "{{ dc_adapter_packages }}"

- name: Create /etc/gtech/pd/config/dc-adapter folder
  file:
    path: /etc/gtech/pd/config/dc-adapter
    state: directory
    owner: springboot
    group: springboot
    mode: 0755

- name: Create /var/log/dc-adapter/ folder
  file:
    path: /var/log/dc-adapter
    state: directory
    owner: springboot
    group: springboot
    mode: 0755


- name: create /usr/local/env
  file:
    path: "/usr/local/env"
    state: directory
    mode: 0755

- name: "Install env to /usr/local/env/"
  template:
    src: "env/dc-adapter"
    dest: "/usr/local/env/dc-adapter"
    owner: "springboot"
    group: "springboot"
    mode: 0755
