---

- name: INIT
  hosts: pdcore
  become: false
  gather_facts: no
  tags:
    - init

  tasks:
    - name: Create local temp directory
      shell: "mktemp -d --tmpdir={{ g_base_tmpdir }} --suffix=_pd-core"
      delegate_to: 127.0.0.1
      run_once: true
      register: reg_tmpdir

    - set_fact:
        p_tmpdir: "{{ reg_tmpdir.stdout }}"

    - debug: msg="Created directory {{ p_tmpdir }}"
      delegate_to: 127.0.0.1
      run_once: true

    - name: Show Connection Infos
      debug: msg="ansible_host={{ ansible_host }}; ansible_user={{ ansible_user }}"

- name: STOP
  hosts: pdcore
  serial: 1
  become: true
  gather_facts: no
  tags:
    - stop

  tasks:
    - name: Stop Service
      systemd:
        name: "pd-core"
        state: stopped
      ignore_errors: yes

- name: CLEANUP CONTAINER
  hosts: pdcore
  become: true
  gather_facts: no
  tags:
    - clean_config

  tasks:
    - name: Deleting Application Folder 
      file:
        state: absent
        path: "/etc/gtech/pd/config/pd-core/main"


- name: INSTALL
  hosts: pdcore
  become: true
  gather_facts: no
  tags:
    - install

  roles:
    - role: ilottery_pd_core
      jboss_node_port_offset: 200



- name: START
  hosts: pdcore
  serial: 1
  become: true
  gather_facts: no
  tags:
    - start

  tasks:
    - name: Start Service
      systemd:
        name: "pd-core"
        state: restarted
        enabled: yes
        daemon_reload: yes

- name: CLEAN
  hosts: pdcore
  become: false
  gather_facts: no
  tags:
    - clean

  tasks:
    - name: Remove local temp directory
      file:
        path: "{{ p_tmpdir }}"
        state: absent
      delegate_to: 127.0.0.1
      run_once: true
